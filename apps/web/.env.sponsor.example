# Copy to apps/web/.env.local for local sponsor flow

# self = fund derived AR address (default)
# sponsor = cashier (mock/Kaspi Worker)
VITE_PUBLISH_MODE=sponsor

# wrangler dev default port
VITE_SPONSOR_URL=http://127.0.0.1:8787
