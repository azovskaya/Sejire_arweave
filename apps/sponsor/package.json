{
  "name": "sejire-sponsor",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit -p tsconfig.json",
    "test": "tsx src/selftest.ts",
    "dev": "echo 'Use: npx wrangler dev (Cloudflare login). Default PAYMENT_PROVIDER=mock.'"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250705.0",
    "tsx": "^4.23.1",
    "typescript": "^5.8.3"
  }
}
